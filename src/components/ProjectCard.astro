---
import Button from './Button.astro'
import Carrousel from './Carrousel.astro'
import GithubLink from './icons/Github-link.astro'
import LinkIcon from './icons/LinkIcon.astro'

interface Props {
	id: string
	title: string
	description: string
	images: string[]
	technologies: string[]
	github?: string
	website: string
}

const { id, title, description, images, technologies, github, website } = Astro.props

const stringTech = technologies.join(' - ')
---

<div
	class='w-[314px] desktop:w-full desktop:max-w-none desktop:justify-self-auto mobile:justify-self-center mobile:max-w-xs rounded-xl backdrop-blur-2xl shadow-[0px_0px_16px_0px_rgba(0,0,0,0.5)] bg-lightgray gradient-custom transition-transform duration-200 hover:scale-105'
>
	<Carrousel images={images} title={title} id={id} />
	<div class='flex flex-col justify-around w-full h-64 px-5 cursor-default'>
		<h2 class='text-xl font-walsheim'>{title}</h2>
		<p class='text-xs text-paragraft'>{description}</p>
		<h3 class='text-xs text-highlight'>{stringTech}</h3>
		<div class='flex items-center gap-4'>
			<Button text='Demo' link={website} gradient='g2' download={false} label='ir a demostración'>
				<LinkIcon />
			</Button>
			{
				github != null ? (
					<a href={github} target='_blank' aria-label='enlace a github'>
						<GithubLink />
					</a>
				) : null
			}
		</div>
	</div>
</div>

<style>
	.gradient-custom {
		background-image: radial-gradient(
			151.92% 127.02% at 15.32% 21.04%,
			rgba(105, 59, 147, 0.2),
			rgba(110, 191, 244, 0.04) 50%,
			rgba(70, 144, 212, 0) 100%
		);
	}
</style>
